#include <stdio.h>
/**
 *main - Entry point
 *Description: 'This function prints a string to the user'
 *Return: always 0 (success)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
